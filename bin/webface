#!/usr/bin/env php
<?php
if (php_sapi_name() !== 'cli') {
    exit;
}
require_once(realpath('./vendor/autoload.php'));
$written = \AbmmHasan\WebFace\Support\Storage::cache();
if ($written) {
    echo "Route cached successfully ({$written} Bytes written)!";
} else {
    echo "Caching failed";
}
exit();